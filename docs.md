# Chameleon change log

## 2026-09-23 — RGB normalization and output bounds

- Request: add a Normalize Brightness toggle that changes extracted color values for brighter, more vibrant LED palettes while leaving the Home Assistant brightness setting alone; ensure every RGB service call stays within standard channel bounds.
- Implementation: added the toggle to new-entry setup and the existing entry's Configure options. When enabled, image and album-art palette colors retain their hue, reach maximum HSV value, and receive a minimum saturation for chromatic colors. Neutral colors become white. Animated gradient steps receive the same treatment. Manual color selection keeps its requested color. Static, animated, and manual light commands now clamp every RGB channel to an integer from 0 to 255 immediately before the service call. The default is off for existing entries.
- Validation: 45 local tests passed, including dark/muted/neutral palette and out-of-range RGB cases; Python compilation, JSON validation, and whitespace checks passed. Live deployment and the enabled-option readback are recorded below once completed.
- Limits: equal RGB maximum values do not ensure equal perceived brightness across different hues or LED hardware. Exact physical matching requires device calibration. Home Assistant's separate brightness control remains unchanged.
- Recovery: disable Normalize Brightness in the Chameleon Configure dialog to restore original extracted colors; reinstall the prior HACS commit if needed. The previous session found that Home Assistant cannot create a full backup until a default backup password is configured.

## 2026-09-22 — Move the live installation to the custom repository

- Request: use the new `DexiTheOne/ha-chameleon-album-art` HACS repository while keeping the existing Common Area Accent Lights setup and automation behavior.
- Baseline: Home Assistant Core 2026.8.3 had one loaded Chameleon config entry from `MKSG-MugunthKumar/ha-chameleon` v3.0.0. Its light, transition number, and transition-style select were `light.chameleon_common_area`, `number.chameleon_common_area_transition`, and `select.chameleon_common_area_transition_style`. The light controlled seven configured lights in the existing order.
- Changed: added the custom HACS repository, downloaded commit `b8b9c8f`, removed the old HACS download, downloaded the custom repository again so it owns the files, and restarted Home Assistant. The existing config entry was retained. No entity registry edits or automation rewrites were needed.
- Verification: Home Assistant configuration check passed before restart. After restart, the integration entry loaded; all three entity IDs retained their original unique IDs, the seven controlled light IDs and order remained the same, transition stayed at 5 seconds, style stayed staggered, and the scene effect list remained available. `chameleon.refresh_scenes` completed successfully. The existing `script.random_colors` and Common Area Accent Scene template helper still reference the retained Chameleon entities. The error log showed no Chameleon setup error; only Home Assistant's standard custom-integration warning.
- Limits: a full Home Assistant backup could not be created because no default backup password is configured. The local Python environment lacks pytest, so the automated suite was not run. A physical scene application was not performed because it would alter multiple currently active lights; the integration's live scene application remains unverified.
- Recovery: HACS still lists the upstream repository and can download v3.0.0 if the custom code fails. Keep the existing config entry and its entity IDs during any rollback. A full Home Assistant backup should be configured before future changes that modify the entity registry or entry data.
