## 2026-09-25 — Retire Chameleon transition modes and animation

- Request: use native WLED transitions exclusively, remove synchronized and staggered modes and continuous animation, and send a zero-second transition unchanged.
- Affected objects: Chameleon light scene, Album Art, manual-color and power behavior; transition controls, config-entry migration, and translations. Existing light identity, configured members, scenes, and WLED blend selector are preserved.
- Action: scene changes use one native WLED request per device. Non-WLED lights receive an immediate color update. The old Transition Style selector and Animation switch are no longer created; migration removes their registry entries and saved options. The slider value is sent to WLED, including zero.
- Validation: Python compilation, JSON parsing, and Git whitespace checks passed. Focused mixed-device routing checks passed with 0 and 1.5-second durations; a zero-second WLED payload regression case was added. Local pytest could not run because pytest is absent. No live Home Assistant deployment was requested or performed, so live behavior remains unverified.
- Limits and rollback: native transitions depend on the WLED device and current effect. Revert this local change before deploying to restore the previous controls.

## 2026-09-25 — Keep scene transitions and fade power changes

- Request: repeated Random or named scene choices can appear to lose their transition, and the chosen transition should apply when Chameleon turns lights on and off.
- Affected objects: Random scene selection, transition controls and options preservation, light power handling, and native WLED power requests. Existing Chameleon entry, entity IDs, configured light order, scenes, and consumers remain unchanged.
- Finding and action: a Random request could select the scene already displayed, producing identical colors and no visible transition. Transition duration, mode, and WLED blend were runtime-only and reset after Home Assistant restarted. Random now excludes the current scene when alternatives exist; the controls save their values in entry options and the options flow preserves them. Power-off uses the transition slider; WLED mode sends one device-level `bs` and `tt` request with every segment off, while other modes send Home Assistant light-off calls with the slider duration. Turning on from off applies the chosen duration to the scene or manual color and avoids an extra parent-light command after a successful native WLED request.
- Validation: Python compilation, JSON parsing, and Git whitespace checks passed. A focused WLED payload test was added. Local pytest collection is blocked by missing `voluptuous` and test dependencies. Live scene testing before this change showed three different named scenes applied in quick succession and the Media Console retained `bs=4`; no native WLED style reset was observed. Home Assistant backup `2720b822` completed before deployment. Published and installed HACS revision `e380044`, read back the changed code, validated configuration, and restarted. Four consecutive Random requests chose four different scenes; a named Aquatic scene applied. Chameleon off left both Media Console segments off with `bs=4`; turning Album Art on restored both segments with distinct assigned colors and `bs=4`. A second restart retained the saved WLED mode and 1.5-second duration; the Outside In style was then saved explicitly and Album Art restored. The entry loaded with the original seven lights, and the error log showed no Chameleon runtime error.
- Limits and rollback: if a named scene is selected twice or two scenes yield identical colors, WLED has no color change to animate. Visual timing cannot be measured through Home Assistant state alone. Reinstall the previous HACS revision `511f174` and restart to restore the prior behavior.

## 2026-09-25 — Correct native WLED blend selection

- Request: multisegment WLED devices still show Fade when Outside In is selected, and the WLED style picker should include the eight one-dimensional modes shown in the device UI.
- Affected objects: native WLED scene request, transition-style options and translations, and focused request tests. The existing Chameleon entry, configured lights/order, scenes, and consumers remain unchanged.
- Finding and action: WLED's own interface writes the device-level JSON state field `bs` for transition style; Chameleon had written segment field `bm`, which is a different setting. The native transition request now writes root-level `bs` once per device and keeps the per-segment color assignments. The picker adds Fairy Dust, Swipe Right/Left, Push Right/Left, and Inside Out alongside Fade and Outside In, using WLED's documented UI values.
- Validation: Python compilation, translation JSON parsing, and Git whitespace checks passed. The focused payload test now asserts root-level `bs` and no segment-level `bm`; local pytest remains unavailable. Home Assistant backup `4364992a` completed. Published and installed HACS revision `e7b8f55`, read back the corrected request and new style IDs, validated Home Assistant configuration, and restarted. The original Chameleon entry loaded with all seven configured lights. Outside In applied Rainbow Gradient and Push Right applied Aquatic; each configured segment reported its assigned color, and the Floor Lamp retained Fairytwinkle. Album Art and Outside In were restored with the 1.5-second slider. The error log showed no Chameleon runtime error. Home Assistant's WLED diagnostics omit the device-level `bs` value, so visual style/readback in WLED's UI remains unverified from this connection.
- Limits and rollback: transition style is device-wide in WLED, so every segment of one device uses the same style. Reinstall the previous HACS revision `ee09f18` and restart to restore the prior behavior.

## 2026-09-25 — Native WLED scene transitions

- Request: add a WLED transition mode and selectable native WLED blend, using the transition slider for scene and Album Art changes while leaving non-WLED lights as immediate color updates.
- Affected objects: the Chameleon transition-style select, a new WLED Transition Style select, scene and Album Art application, and WLED JSON requests. Existing config entry, light IDs/order, scene names, and animation behavior in the other modes remain unchanged.
- Action: WLED mode sends each configured WLED device one JSON state request containing all of its segments, each configured segment's assigned image color followed by image-derived color slots, brightness, a one-request `tt` fade, and the selected blend mode (Outside In by default, or Fade). Device requests are launched concurrently. Existing effect and palette IDs are preserved. The continuous Chameleon animation loop is bypassed in this mode; non-WLED lights receive their new colors with zero transition. Switching into or out of WLED mode reapplies the active scene.
- Validation: Python compilation, translation JSON parsing, and Git whitespace checks passed. Added focused tests for multi-segment native transition payloads and per-segment colors; local pytest is unavailable. A fresh Home Assistant backup completed before deployment (backup ID `3038a016`). Published and installed HACS revision `2e550d0`, read back its changed code, validated Home Assistant configuration, and restarted. The original Chameleon entry loaded with all seven configured lights and the Random Colors script and Common Area Accent Scene helper remained present. WLED Outside In applied Rainbow Gradient to all seven lights; each configured segment read back its assigned color and the Floor Lamp retained Fairytwinkle. Fade applied Aquatic with the same seven-light readback. Chameleon off turned all seven lights off; Album Art turned all seven on again. The Desk Strip state lagged briefly before reporting on. The transition slider was restored to 1.5 seconds and the final state is WLED / Outside In / Album Art. The error log showed no Chameleon runtime error after restart.
- Limits and rollback: WLED has no documented cross-device prepare-and-commit transition trigger for unique per-device colors. Concurrent HTTP calls minimize start skew but network and device latency can still differ. A device whose current effect ignores color slots may not visibly adopt the new image colors; the request intentionally preserves its effect. WLED blend mode persists on each device until changed. Revert this revision to restore the prior transition modes, or select Synchronized/Staggered to stop using the new WLED path.

# Chameleon change log

## 2026-09-25 — Fade one-LED WLED segments

- Request: spatial WLED transition styles look poor on segments with only one LED; use Fade for those lights without changing the transition duration.
- Affected objects: native WLED scene, album-art, turn-on, and turn-off requests. Existing Chameleon entry, entity IDs, configured order, scenes, and consumers remain unchanged.
- Finding and action: the live Sunset Lamp has one LED and the Flood Lights have two one-LED segments; the Media Console has two 84-LED segments. WLED's `bs` style applies to the whole device, so Chameleon now sends Fade (`bs=0`) whenever any segment reports one LED, while preserving the selected `tt` duration. Other devices retain the selected style.
- Validation: Python compilation and Git whitespace checks passed. A focused payload test checks one-LED fallback on scene and power-off requests. Local pytest collection lacks `voluptuous`. Home Assistant backup `07bdb38a` completed before deployment. Published and installed HACS revision `5477feb3cf3594333d197f589c5c258a72f9b533`, read back the installed fallback, validated configuration, and restarted. On a live Album Art request and again after turning Chameleon off, the Sunset Lamp and both Flood Light segments reported `bs=0`, while both 84-LED Media Console segments retained Outside In (`bs=4`). Album Art and all seven configured lights were restored on; the recent log contained no Chameleon warning or error.
- Limits and rollback: mixed-length devices fade on every segment because WLED cannot set different transition styles per segment. Reinstall prior HACS revision `97147c16ea048e9f2d5ddb4483537316e429a62d` and restart to restore previous behavior.

## 2026-09-25 — Synchronize outside-in WLED fades

- Request: make WLED palette changes fade outside-in for the configured transition duration, and start that fade early enough to finish with the other lights.
- Affected objects: WLED JSON segment blend mode and scene timing. The Chameleon entry, entities, light order, scene choices, and consumers remain unchanged.
- Action: set WLED segment blend mode 4 (Outside-in) when supported, send the three image-derived color slots with one-call transition `tt`, and start that request alongside the ordinary light fade. For staggered animation, align it with the first animated fade. Effect and palette selections remain untouched.
- Validation: Python compilation and diff checks passed. Pushed and installed `38d6c5c`, read back both changed files, validated Home Assistant configuration, and restarted. A live Rainbow Gradient scene set the Floor Lamp to blend mode 4 with three scene colors while retaining effect 51 and palette 5. Restoring Album Art put three current image colors in its slots with those settings intact; Chameleon retained all seven configured lights. One transient `JSONDecodeError` warning occurred while reading the Desk Strip, which continued through ordinary Home Assistant light control. The local Python environment lacks pytest. Visible fade direction and synchronization cannot be measured through the JSON state alone.
- Limits and rollback: the outside-in blend mode persists on WLED until changed. Reinstall HACS revision `8103716` and restart to restore the earlier request timing and blend mode behavior; WLED blend mode can then be returned to its previous setting in the device UI.

## 2026-09-25 — Fade WLED palette color changes

- Request: the Floor Lamp's palette colors changed abruptly even with WLED's Fade palettes setting enabled.
- Affected objects: the WLED JSON palette request and Chameleon's scene and Album Art calls. Existing entities, configured light order, scenes, switch, and consumers remain unchanged.
- Finding and action: the Floor Lamp's WLED state reported a 0.7 second default transition, while Chameleon used 1.5 seconds. The JSON request changed color slots without a per-request transition. Chameleon now sends WLED's `tt` field using the configured Chameleon transition duration after the ordinary light fade completes. The JSON request still does not select an effect or palette.
- Validation: Python compilation, JSON/whitespace checks, and a focused request-payload test are planned. Live installation will be read back and checked. The local Python environment lacks pytest.
- Limits and rollback: WLED controls how each effect displays transitions, so the visible result can vary by effect. Reinstall HACS revision `f86658e` and restart to restore the old JSON request; turning off Send Palette information to WLED stops these requests.

## 2026-09-25 — Send WLED colors after the scene fade

- Request: when Album Art changes its source image or a scene is selected, send WLED palette information after the transition fade.
- Affected objects: Chameleon's static and animated palette sequence and WLED eligibility check. The existing entry, entities, configured light order, scenes, and consumers are preserved.
- Action: check which WLED devices already have a compatible palette and effect without writing colors, begin the ordinary light transition, wait for its configured duration, then send the image-derived WLED color slots to every segment. Brief empty WLED JSON reads are retried. The animated scene path waits for its first fade, including the staggered start. Album Art performs its initial fade before starting continuous animation and avoids a duplicate palette send. Compatible WLED lights stay out of the ordinary RGB calls and animation so their current effect remains selected.
- Validation: Python compilation and whitespace checks passed; a focused palette preflight test was added. Initial live deployment of `93ab155` passed a Rainbow Gradient scene test on the Floor Lamp, retaining effect 51 and palette 5 with three source colors. An Album Art test exposed intermittent WLED `JSONDecodeError` warnings and left two old slots, prompting the retry correction. Pushed and installed `f86658e`; HACS needed the full commit ID to put the latest file on disk. Read back the installed retry code, validated Home Assistant configuration, and restarted. A Rainbow Gradient to Album Art change then filled all three Floor Lamp slots with the single current artwork color `(89, 172, 255)` while preserving effect 51 and palette 5. The Chameleon light kept its seven configured members and reported Album Art; the system log had no Chameleon errors. The local Python environment lacks pytest. A full Home Assistant backup remains unavailable because no default backup password is configured.
- Limits and rollback: a WLED device with no configured color-slot palette or palette-aware effect receives no JSON color update. Reinstall HACS revision `6246545` and restart Home Assistant to restore the previous send order; turning off Send Palette information to WLED stops direct palette requests immediately.

## 2026-09-25 — Fill every WLED palette slot from the image

- Request: when artwork yields fewer than three distinct colors, repeat its colors across all three WLED slots so older colors cannot remain visible; push and update the live installation.
- Affected objects: WLED color-slot selection and focused tests. The Chameleon entry, switch, configured light order, scenes, and consumers are unchanged.
- Action: a one-color image supplies that color to all three slots; a two-color image alternates its colors across the slots. Empty palettes still send nothing. WLED devices still need an already configured color-slot palette and palette-aware effect, and the JSON request leaves effect and palette settings alone.
- Validation: local compilation, JSON parsing, whitespace checks, five focused slot-filling cases, and a one-color JSON payload check passed; pytest is unavailable locally. Before deployment, live Album Art exposed one orange color `(255, 122, 38)`. The Floor Lamp already had a palette-aware effect (51) and Colors Only palette (5), but its three WLED slots were orange, purple, and blue. Published and installed HACS revision `a48739f`, read back the updated installed Python file, validated Home Assistant configuration, and restarted. Reapplying Album Art made all three Floor Lamp slots `(255, 122, 38)` while preserving effect 51 and palette 5. All seven configured lights reported orange, the other devices remained on Solid, and the Chameleon system log had no errors.
- Rollback: reinstall HACS revision `75f7966` to restore the prior three-distinct-color requirement; turn off Send Palette information to WLED to stop direct WLED requests.

## 2026-09-25 — Keep WLED colors faithful to artwork

- Request: the current orange artwork left green and blue visible on WLED lights; skip palette requests for single-color artwork or devices without a configured palette, use only source-image colors, and preserve each device's effect.
- Affected objects: WLED palette selection and Chameleon's static and animated scene application. The existing integration entry, switch, light IDs, configured order, and consumers remain unchanged.
- Finding and action: Album Art exposed one orange color, while the previous WLED helper generated green and blue to fill the other slots and forced WLED's Palette effect. The helper now requires three distinct extracted colors and never synthesizes hues. It sends colors only when every segment already has palette 4 or 5 and an effect whose WLED metadata explicitly uses palettes. Its JSON request changes only color slots and brightness. For eligible WLED devices, Chameleon skips its ordinary RGB calls and animation so the existing WLED effect stays selected; all other lights use the existing Home Assistant light path. The live switch was turned off and all five devices were returned to Solid with only the current orange in their color slots to clear the stale hues before deployment.
- Validation: local compilation, JSON parsing, whitespace checks, and focused source-color and request-payload checks passed; pytest is unavailable locally. Published and installed HACS revision `43e4838`, confirmed the updated Python file on Home Assistant, validated configuration, and restarted. The original Chameleon entry and seven controlled light IDs remained intact. With the switch on, a multicolor Album Art update left the Media Console on Solid with palette 0, confirming that a device without a configured palette receives no WLED palette request. The WLED request logic was checked locally to contain only three source swatches and no `fx` or `pal` fields. A direct live eligible-device test was stopped by automatic approval review because changing a device's current effect for test setup conflicted with the request to preserve effects. One Sunset Lamp eligibility read logged a transient `JSONDecodeError`; ordinary Home Assistant color application continued.
- Limits and rollback: devices using another WLED palette type are treated as ineligible because those palettes do not use all three color slots. The configured-palette path has not been exercised live. Reinstall HACS revision `210927a` to restore the previous behavior; turn the switch off to stop WLED palette requests immediately.

## 2026-09-24 — Send scene colors to WLED

- Request: add a toggle named Send Palette information to WLED and send three distinct colors per WLED light through the JSON API. Colors may repeat across different lights.
- Affected objects: a new Chameleon switch, scene and album-art palette application, WLED JSON requests, option preservation, and translations. Existing Chameleon entity IDs, configured light order, scenes, and consumers are unchanged.
- Actions: the switch defaults off and persists in config entry options. When on, Chameleon identifies configured WLED light entities through Home Assistant's entity registry and WLED config entries, reads each WLED device's current segment IDs, then posts three colors to every segment after applying a scene. A device is sent one palette even when several of its segment entities are configured. Each device gets a rotated set of source colors; missing distinct colors are filled with derived hues. Non-WLED lights continue through normal Home Assistant light calls. Manual color commands remain unchanged.
- Validation: Python compilation, translation JSON parsing, and Git whitespace checks passed. The local Python has no pytest. Before deployment, the live Chameleon entry was loaded with the original seven lights, and Home Assistant's WLED entity registry confirmed segment IDs 0 and 1 for the two multisegment devices. A predeployment backup was attempted; Home Assistant rejected it because no default backup password is configured. Live deployment and physical WLED readback remain to be completed. The first commit could not be pushed: sandbox DNS failed, then automatic approval review rejected a direct push to the remote main branch because deployment authorization did not clearly establish ownership or approve that mutation. Local GitHub CLI authentication also reports an invalid token.
- Limits and rollback: the first configured entity for a WLED device determines that device's palette when several of its segment entities are selected. Network failures are logged without stopping the ordinary light command. Turn the switch off to stop JSON requests, or revert this change to remove the switch.

## 2026-09-25 — Make the WLED palette active

- Request: the WLED web UI still said the color palette was not used after receiving three colors.
- Affected objects: WLED JSON requests only; the Chameleon device, switch, config entry, and configured light order remain the same.
- Finding and action: live JSON readback confirmed three distinct colors on both segments of the Media Console and Flood Lights, but each segment still ran the Solid effect with palette ID 0. Solid ignores palettes. When the toggle is on, Chameleon now discovers WLED's Palette effect and selects it with the Colors Only palette (ID 5) while sending the three color slots to every segment.
- Validation: before this adjustment, HACS installed revision `095b7ae`, the switch appeared off by default, a Rainbow Gradient scene produced distinct colors on all segments of both multisegment devices, and Chameleon reported no error. Published `9cfa150` and installed it through HACS. The first HACS download reported that revision but left the previous Python file in place; a second download replaced the file, confirmed by reading it back, and Home Assistant was restarted again. With the switch on and Album Art reapplied, all five WLED devices reported effect 65 (Palette), palette 5 (Colors Only), and three distinct colors on every active segment, including both segments of the Media Console and Flood Lights. The original Chameleon entry and seven configured light IDs remained intact, and the Chameleon system log had no errors.
- Limits and rollback: WLED devices without a Palette effect are skipped and logged. Turning the switch off stops these JSON changes but does not restore a previous WLED effect automatically. Reinstall revision `095b7ae` to restore color-slot-only behavior.

## 2026-09-23 — Spread white across mostly white artwork

- Request: for the supplied Beijing Music Radio image, show white on multiple light groups and red on only a few while Only Interesting Colors is on.
- Affected objects: the shared album-art/image-scene palette filter and distribution, its focused tests, and README. The existing integration entry, entity IDs, seven-light order, scenes, and consumers remain unchanged.
- Implementation: sample actual image pixels because palette extraction can omit white. When at least 70% of pixels are bright neutral white and no more than one accent hue survives filtering, assign white to most lights and the accent to at most two lights; a seven-light setup gets five white and two accent assignments. Keep the existing palette behavior for colorful images.
- Validation: the supplied image measures 89.3% bright neutral white at sample size; a direct seven-light check gives five white and two red assignments. Python compilation and Git whitespace checks passed; pytest is unavailable locally. Pushed commit `32752d0`, installed that exact HACS revision, validated Home Assistant configuration, and restarted. The original Chameleon entry loaded with the same seven controlled lights and the filter still on. Reapplying Album Art to the artwork now playing produced a one-color blue palette and updated all seven lights; each reported blue afterward, and no Chameleon system-log error appeared. The supplied image was not served by the player during live verification, so its white/red distribution was verified locally only.
- Limits and rollback: sampling and hue filtering approximate the artwork. A full Home Assistant backup was attempted but rejected because no default backup password is configured. Reinstall the previous HACS revision `ecc0cc7` and restart Home Assistant to restore the old behavior; turning off Only Interesting Colors bypasses the filter immediately.

## 2026-09-23 — Accept Eversolo artwork from other playback sources

- Request: use the album artwork now exposed by the DMP-A6 integration for sources such as Squeeze Connect.
- Affected objects: Chameleon's Album Art artwork gate and README. The existing config entry, entity IDs, seven light order, scenes, and automation consumers are unchanged.
- Finding and action: the live Chameleon light was on Album Art with `media_player.eversolo_dmp_a6` selected and reported `Configured media player is showing fallback artwork`, while that player was playing and exposed an `entity_picture`. Chameleon inspected Eversolo's private coordinator fields and accepted only its older internal-player and Spotify cover fields. Removed that source-specific gate so it accepts the media player's Home Assistant artwork field. Existing off/idle, absent-picture, recognized-placeholder, download-size, and palette checks remain.
- Validation: local Python compilation and Git whitespace checks passed. The local Python environment has no pytest. After explicit authorization, pushed commit `ecc0cc7`, installed that exact HACS revision, and restarted Home Assistant. The live Eversolo was playing `FM974 Beijing Music Radio` with an `entity_picture`; Chameleon selected Album Art with no `last_error` and assigned `(255, 42, 42)` to all seven configured lights. Each member reported on with that color (the floor lamp reported `(255, 43, 43)`). A separate track or artwork change was not observed during verification.
- Limits and rollback: An opaque Home Assistant proxy URL cannot reveal whether Eversolo serves a generic cover, so an unrecognized default image may be accepted. Reinstall HACS revision `7ebd799` and restart Home Assistant to restore the prior gate. A full Home Assistant backup was previously unavailable because no default backup password is configured.

## 2026-09-23 — Allow white from neutral or mostly white artwork

- Request: let black-and-white or mostly white source images use white LEDs while Only Interesting Colors is enabled, without showing white for colorful artwork.
- Affected objects: the shared album-art and image-scene palette filter, its focused tests, and README. Existing switches, config entry, entity IDs, scenes, and light order are unchanged.
- Implementation: retain the first bright neutral swatch when no useful hue survives, or when white is among the first two dominant swatches and at most one distinct hue survives. Keep its original dominance position and continue excluding white when several useful hues are present.
- Validation: Python compilation, Git whitespace checks, and four direct palette behavior checks passed. The local Python environment lacks pytest, so the focused test suite could not run. No live Home Assistant deployment or entity readback was performed.
- Limits and rollback: Palette order approximates image dominance; a small number of extracted swatches cannot measure exact white pixel coverage. Revert this filter change to restore the previous white exclusion, or turn off Only Interesting Colors to bypass filtering immediately.

## 2026-09-23 — Keep clear pastels and reduce skin-tone dominance

- Request: the vivid-color filter removed an obviously blue album background yet allowed face colors to dominate other covers.
- Affected objects: Chameleon's shared image/album-art palette filter and its tests. The existing switch, config entry, entity IDs, scene names, and configured light order are preserved.
- Implementation: lowered the source saturation floor only when RGB channel separation shows a definite hue, so near-white and gray remain excluded. Muted tan/peach tones in the typical skin hue range are rejected while vivid orange and yellow remain eligible. Repeated hues are collapsed in dominance order so multiple near-identical face or text shades cannot fill the palette. Existing Normalize Brightness can then raise retained pale blue to a bright LED blue.
- Validation: all 60 local tests passed, including focused pale-blue and skin-tone cases. Reprocessing the captured `Kills You Slowly` cover with the installed ColorThief version yields blue `(201, 228, 244)` and coral `(231, 161, 147)` before brightness normalization, then LED colors `(89, 193, 255)` and `(255, 117, 89)`. Pushed commit `7ebd799`, installed that exact HACS revision, read back the new live filter, and restarted Home Assistant. Turned Only Interesting Colors back on and applied Album Art. When `Kills You Slowly` played again, the live palette was blue `(89, 193, 255)` and coral `(255, 117, 89)`, with both colors reported by physical lights. A subsequent track update reached all seven lights with no error. The floor lamp was temporarily unavailable immediately after restart, causing a partial failure, then recovered. A captured face-containing `Dreamer in Concert` cover yielded one orange and one teal accent locally after filtering, instead of repeated warm/neutral shades.
- Limits and rollback: Skin-tone detection is based on color ranges, not face recognition, so unusual skin lighting and similarly colored artwork may be classified differently. The face-containing cover was tested locally, not confirmed live on the lights before the rapidly changing playlist advanced. Reinstall the prior HACS revision `80dca84` and restart to restore the old filter behavior; turning off Only Interesting Colors bypasses the filter immediately.

## 2026-09-23 — Preserve pale blue in the current album cover

- Request: compare the current Squeezebox cover with the assigned light colors after no blue appeared.
- Affected objects: `switch.chameleon_common_area_interesting_colors`, the active Album Art effect, and seven configured lights. No code or entity IDs changed.
- Findings and action: The current `Kills You Slowly` cover is predominantly pale blue with smaller coral text areas. Sample blue pixels are about `(205, 234, 248)` with HSV saturation around 0.17, below the switch's 0.28 minimum and also below its pale-highlight threshold. With the filter on, Chameleon reported four orange palette colors. Turned the filter off and reapplied Album Art; the palette became three blue and four coral colors.
- Validation and limits: All seven physical lights reported their assigned colors after the reapply, including three blue lights. The current cover was fetched from Home Assistant's media-player proxy and visually inspected. The filter remains off, so future covers may also include pale or muted colors.
- Rollback: Turn on `switch.chameleon_common_area_interesting_colors` and reselect Album Art to restore the prior vivid-only palette behavior.

## 2026-09-23 — Read the complete Squeezebox artwork response

- Request: Album Art remained selected while Squeezebox Boom played, but light colors did not update.
- Affected objects: Chameleon's bounded album-art downloader. Config entry, entity IDs, scenes, and light order are unchanged.
- Findings and action: Live readback found Boom playing with an `entity_picture` and the Chameleon light reporting an extraction error. The Home Assistant system log gave `image file is truncated (6 bytes not processed)`. The downloader made one `aiohttp` stream `read(n)` call and treated its result as the entire image, although that call can return fewer bytes than requested before EOF. It now consumes the response in chunks through EOF and enforces the 10 MB limit after each chunk. A genuine malformed source image still raises an extraction error.
- Validation and limits: Python compilation and diff checks passed; pytest is unavailable locally. Pushed commit `80dca84`, confirmed HACS installed that exact revision and the live file contains the chunked reader, then restarted Home Assistant. With Boom playing, selecting Album Art extracted a palette and updated all seven physical lights with no `last_error`. After the user advanced the track, Chameleon automatically extracted a new two-color palette, updated its timestamp, and all seven lights reported the assigned new colors. The system log showed no new album-art extraction error. The upstream and proxy image bytes were not independently captured, so their own integrity remains unverified. No full backup was taken because the prior HACS commit is available for a targeted rollback; the earlier backup attempt was rejected by Home Assistant because no default backup password is configured.
- Rollback: Reinstall the prior HACS revision `8b6751a` and restart Home Assistant.

## 2026-09-23 — Deploy and verify combined controls

- Request: push the combined local changes, update the live HACS integration, and test it.
- Affected objects: the existing Chameleon integration entry, its scene light and existing controls, plus new Animation and Only Interesting Colors switches. The configured light order and existing entity IDs were preserved.
- Actions: reviewed the combined diff, corrected switch and Configure option preservation, guarded empty palette results, committed and pushed the integration to the private GitHub remote, installed that commit with HACS, checked Home Assistant configuration, and restarted Home Assistant. A pre-change backup was attempted but Home Assistant rejected it because no default backup password is configured. The prior HACS commit remains available for rollback.
- Validation: the integration reloaded; HACS reported the installed commit; both new switches appeared on the same device. The color filter persisted its option when toggled. With Animation off, a live image scene applied a static palette with no running animation; the filter reduced a second scene palette from seven swatches to six. The original transition, style, Random assignment setting, script and helper references, and all controlled-light states were read back. The lights were restored to their recorded on/off states and colors, and Animation was returned to on. No Chameleon error was found in the recent Home Assistant error log. Local Python compilation, JSON parsing, and whitespace checks passed; pytest is unavailable in both local Python environments.
- Limits and rollback: the configured artwork source was off, so a real album-art change could not be tested. The filter was left on to fulfill the requested behavior. Turn it off to restore all extracted swatches, or reinstall the prior HACS commit and restart to roll back the integration.


## 2026-09-23 — Only Interesting Colors control

- Request: add a device control that selects vivid colors from album art and image scenes.
- Affected objects: new `switch.chameleon_*_interesting_colors` on each Chameleon device and both extracted palette paths. Existing config entries, light IDs, scene names, and light order are preserved.
- Implementation: the switch defaults off and saves its state in config entry options. After RGB channel normalization, HSV thresholds reject near neutral, very dark, and pale highlight swatches in their existing dominance order. Filtering runs before optional Normalize Brightness. A scene or artwork with no surviving colors leaves the current lights unchanged and reports an error. Single-light image scenes use a palette while the switch is on so a dull dominant color can be skipped. Manual colors are unaffected.
- Validation: Python compilation, translation JSON parsing, and whitespace checks passed. The local Python environments lack pytest, so automated tests could not run; live deployment remains to be verified.
- Rollback: turn off the switch to restore the original palette selection; reinstall the prior integration revision to remove the control.


## 2026-09-23 — Separate animation from transition time

- Request: add an Animation toggle so nonzero transition time can fade between scenes and album-art palettes without ongoing color cycling.
- Affected objects: new `switch.chameleon_*_animation` on each Chameleon device; transition number, scene effect, and Album Art behavior. Existing scene and light entity IDs and configured light order are preserved.
- Implementation: Animation defaults on for existing entries to preserve prior behavior and is stored in config-entry options when changed. With Animation off, each palette is applied using the transition number as a single Home Assistant light fade. Valid artwork updates use the same fade. The animation manager remains stopped afterward. The toggle re-applies the active scene when changed.
- Validation: Python compilation, translation JSON parsing, and Git whitespace checks passed. The focused pytest suite could not run because pytest is absent from the local Python environment. No live Home Assistant deployment or entity readback was performed; a Home Assistant MCP connection is not available in this task.
- Limits and rollback: physical bulbs may implement fade timing differently. Reinstall the previous integration revision to restore the former behavior; existing options and entity IDs remain intact.

## 2026-09-23 — Randomize Album Art color assignment on updates

- Request: when Randomize Color Assignment is on, assign Album Art palette colors to lights in a new random order each time valid artwork updates.
- Implementation: Album Art now uses the existing non-repeating shuffle helper after a valid cover has been downloaded and its palette extracted. Static application and animated starting positions use the same order. A transition or style reapply retains the current order; missing/default artwork leaves it unchanged. With the switch off, Album Art uses the configured light order.
- Affected objects: Chameleon Album Art effect and its existing Randomize Color Assignment switch. Config entries and light entity IDs are unchanged.
- Validation: Python compilation and diff checks. The configured live artwork source is Squeezebox Boom, currently off, and the local code has not been deployed; a live album update was not tested. Pytest is unavailable in this workspace.
- Rollback: restore the prior integration revision to make Album Art use configured light order regardless of the switch.

## 2026-09-23 — Apply new artwork colors immediately

- Request: show each valid new cover's colors immediately and leave the prior colors alone when artwork disappears or becomes a default image.
- Implementation: on a valid artwork change, Chameleon now sends the new palette to the lights before restarting an enabled animation. This removes the animation's initial stagger delay and slow first fade from the cover-change response. Missing pictures, inactive media-player states, and recognizable placeholder paths skip extraction and light commands. For the Eversolo integration, the source coordinator distinguishes an explicit `albumArt`/Spotify icon from the song-ID fallback that may serve default art; Chameleon ignores that fallback. The Album Art effect remains selected so a later real cover can update it.
- Affected objects: Chameleon Album Art effect only; no entity IDs, configured lights, or media-player options changed.
- Validation: local Python compilation and diff checks. Live readback found Chameleon's artwork source still set to Squeezebox Boom, which is currently off, while the DMP-A6 entities are `media_player.eversolo_dmp_a6` and `media_player.eversolo_music_player`. The latter is unavailable. The DMP-A6 integration's `media_image_url` returns direct `albumArt` for internal playback when available, then falls back to a song-ID image URL. Home Assistant recorder omits `entity_picture`, so recorded states cannot establish whether past tracks had art. No real-cover transition was tested; the code has not been deployed. Automated tests remain unavailable because pytest is not installed in this workspace.
- Limits and rollback: Eversolo artwork available only through its song-ID fallback is skipped, even if that fallback sometimes returns a real cover. Other players with opaque proxy URLs and no explicit placeholder signal may still supply a default image. Restore the previous integration revision to revert this behavior.

## 2026-09-23 — Keep Album Art selected and exclude it from Random

- Request: make Album Art stay active until manually changed, keep it out of Random, and investigate intermittent skipped selections.
- Cause and implementation: Album Art was recorded as the active effect only after artwork download, extraction, and light application succeeded. A temporary missing picture or download failure therefore left the prior effect active, allowing its animation to continue. Reapplying the same artwork also returned early, so transition/style changes could leave no active animation. Album Art selection now stops the prior animation, records the effect immediately, and forces a fresh apply. Later artwork changes retry while Album Art remains selected. Choosing another effect deactivates Album Art immediately. Random excludes reserved effects, and reserved names are excluded from the image scene scan. The Off effect path also avoids acquiring the same lock twice.
- Affected objects: Chameleon light effect selection and image scene cache. Existing config entries, entity IDs, and light order are unchanged.
- Validation: Python compilation and Git diff checks completed. The live Chameleon light was read back as off, with Album Art in its effect list and the configured source `media_player.squeezebox_boom`. That player was also off and had no current `entity_picture`, which explains why selecting Album Art at that moment cannot produce new colors. Recent Home Assistant system/error logs showed no Chameleon runtime exception; the only matching warning was the standard custom-integration loader notice. Automated tests could not run because pytest is not installed in this workspace. The new code was not deployed, so live behavior after this change remains unverified.
- Limits and rollback: an unavailable or invalid artwork source still cannot provide new colors; the last applied colors stay in place and `last_error` reports the cause. Revert this commit or reinstall the prior integration version to restore previous behavior.

## 2026-09-23 — Move Random assignment toggle into device controls

- Request: show Randomize Color Assignment as a control entity instead of a setting in Configure.
- Implementation: added a switch entity to each Chameleon device and removed the toggle from both setup and Configure forms. The switch reads the existing saved option, writes changes through the Home Assistant config-entry API, and updates the active Chameleon light immediately for the next Random selection. Configure preserves that saved option when other settings change. Existing device and light entity IDs stay the same.
- Validation: 53 local tests passed, including switch persistence and removal from both setup and options forms; Python compilation, translation JSON validation, and diff checks passed. HACS installed commit `0334203` on Home Assistant Core 2026.8.3. After restart, `switch.chameleon_common_area_randomize_color_assignment` appeared on the existing Common Area Accent Lights device in the on state. Turning it off and back on through Home Assistant updated the switch state, Chameleon light attribute, and saved config-entry option in lockstep. The original Chameleon light, transition number, transition-style select, album-art source, and Normalize Brightness option remained present. No Chameleon setup error appeared in the log.
- Recovery: the prior HACS commit can be reinstalled without changing the saved option; the previous Configure toggle will read its value. No entity or device removal is required.

## 2026-09-23 — Random scene color assignment

- Request: provide a toggle that changes the light-to-palette order each time Random chooses an image.
- Implementation: added Randomize Color Assignment to the Chameleon setup and Configure options. When enabled, a Random invocation chooses an image and shuffles a copy of the configured light list. The shuffled list controls static palette assignment and animated starting-color order. A repeated Random invocation avoids reusing the previous order when more than one light is available. Named scenes and Album Art use the configured order. The chosen assignment survives transition or style reapplication of the active scene.
- Validation: 50 local tests passed, including unchanged configured light order, shuffled assignments, repeat selection, and single-light behavior. HACS installed commit `8125e7b` on Home Assistant Core 2026.8.3. The existing entry retained its album-art source, Normalize Brightness option, seven controlled-light IDs, and transition-style setting. The Randomize Color Assignment option was enabled and read back as `true`. Two live Random selections chose different images and produced different seven-light assignment orders with all seven members present. Applying named scene Aquatic returned to the configured order. No Chameleon error appeared in the log.
- Test cleanup: the temporary scene did not turn three previously off lights back off. Those three lights were turned off individually after the Chameleon transition was returned to its pre-update value of 0 seconds. Final readback matched the pretest on/off states for all seven lights; Chameleon was on Aquatic with no animation, as before the deployment restart. For future tests, verify each light after scene restoration and correct any missed off states directly.
- Recovery: disable the toggle in Configure to return Random to the configured order. No entity IDs, unique IDs, or controlled-light configuration are changed.

## 2026-09-23 — RGB normalization and output bounds

- Request: add a Normalize Brightness toggle that changes extracted color values for brighter, more vibrant LED palettes while leaving the Home Assistant brightness setting alone; ensure every RGB service call stays within standard channel bounds.
- Implementation: added the toggle to new-entry setup and the existing entry's Configure options. When enabled, image and album-art palette colors retain their hue, reach maximum HSV value, and receive a minimum saturation for chromatic colors. Neutral colors become white. Animated gradient steps receive the same treatment. Manual color selection keeps its requested color. Static, animated, and manual light commands now clamp every RGB channel to an integer from 0 to 255 immediately before the service call. The default is off for existing entries.
- Validation: 46 local tests passed, including dark/muted/neutral palette, animated gradient, and out-of-range RGB cases; Python compilation, JSON validation, and whitespace checks passed. HACS installed commit `c06864a` on Home Assistant Core 2026.8.3. After restart the original integration entry and seven controlled light IDs remained loaded. The live `normalize_brightness` option was enabled and read back as `true`; the transition stayed at 5 seconds and the style stayed staggered. A brief static Aquatic scene test produced seven bounded RGB triples, each with a maximum channel value of 255. The temporary scene restored the controlled lights, Chameleon returned to off, and the transition was reset to 5 seconds. No Chameleon error appeared in the log.
- Limits: equal RGB maximum values do not ensure equal perceived brightness across different hues or LED hardware. Exact physical matching requires device calibration. Home Assistant's separate brightness control remains unchanged.
- Recovery: disable Normalize Brightness in the Chameleon Configure dialog to restore original extracted colors; reinstall the prior HACS commit if needed. The previous session found that Home Assistant cannot create a full backup until a default backup password is configured.

## 2026-09-22 — Move the live installation to the custom repository

- Request: use the new `DexiTheOne/ha-chameleon-album-art` HACS repository while keeping the existing Common Area Accent Lights setup and automation behavior.
- Baseline: Home Assistant Core 2026.8.3 had one loaded Chameleon config entry from `MKSG-MugunthKumar/ha-chameleon` v3.0.0. Its light, transition number, and transition-style select were `light.chameleon_common_area`, `number.chameleon_common_area_transition`, and `select.chameleon_common_area_transition_style`. The light controlled seven configured lights in the existing order.
- Changed: added the custom HACS repository, downloaded commit `b8b9c8f`, removed the old HACS download, downloaded the custom repository again so it owns the files, and restarted Home Assistant. The existing config entry was retained. No entity registry edits or automation rewrites were needed.
- Verification: Home Assistant configuration check passed before restart. After restart, the integration entry loaded; all three entity IDs retained their original unique IDs, the seven controlled light IDs and order remained the same, transition stayed at 5 seconds, style stayed staggered, and the scene effect list remained available. `chameleon.refresh_scenes` completed successfully. The existing `script.random_colors` and Common Area Accent Scene template helper still reference the retained Chameleon entities. The error log showed no Chameleon setup error; only Home Assistant's standard custom-integration warning.
- Limits: a full Home Assistant backup could not be created because no default backup password is configured. The local Python environment lacks pytest, so the automated suite was not run. A physical scene application was not performed because it would alter multiple currently active lights; the integration's live scene application remains unverified.
- Recovery: HACS still lists the upstream repository and can download v3.0.0 if the custom code fails. Keep the existing config entry and its entity IDs during any rollback. A full Home Assistant backup should be configured before future changes that modify the entity registry or entry data.

## 2026-09-25 — Toggle WLED main lights with Chameleon

- Request: make `light.chameleon_common_area` turn multi-segment WLED devices fully on and off when their configured segment lights are controlled.
- Affected objects: Chameleon light on/off handling and WLED entity lookup. The existing Chameleon config entry, entity IDs, configured light order, scenes, and consumers are unchanged.
- Action: find each WLED main light from the configured segments' registry unique IDs and verify that it belongs to the same device and config entry. Apply segment colors before turning the main light on, including a first color for asynchronously started animations, so the previous color is not revealed. Turn the main light off after turning off the segments. Each main light is addressed once, including when several segments from one device are configured. A main light already in the configured list is not addressed twice.
- Validation: inspected the live WLED entity registry: the Media Console main light uses unique ID suffix `_0`, and its segment light uses `_1` on the same device and config entry. Added a focused lookup test. Python compilation and Git whitespace checks passed. The local Python environment lacks pytest. This code has not been deployed or physically tested on the lights.
- Limits and rollback: this lookup applies to WLED entities using the observed `_0` main and numbered segment unique-ID convention. Revert this change to restore segment-only on/off behavior.

## 2026-09-25 — Native random scene button and mirrored scene select

- Request: replace the random-colors script functionality with a Choose Random Scene button and expose a Scene select under device Controls that mirrors the light effect.
- Affected objects: new button platform, shared scene-control support, existing select platform, platform registration, English entity names, README, and focused tests. Default Common Area IDs are `button.chameleon_common_area_choose_random_scene` and `select.chameleon_common_area_scene`, subject to registry collision handling.
- Actions: both controls use the registered Chameleon light directly, including its actual entity ID after renaming. The button invokes Random through the normal turn-on path, retaining brightness/transition and existing WLED parent handling. The select reads the light's current effect and effect list and subscribes to its state changes with automatic listener cleanup. Random resolves to the chosen scene; manual color/off follow the light's effect. No duplicate scene state is stored. The button is unavailable when there are no image scenes. Existing helpers, group, script, dashboards, and automations have not been removed or rewritten.
- Validation: all five focused scene-control tests passed. Full local suite: 77 passed, one existing WLED test failed with an undefined `duration` variable in `test_single_led_segment_fades_on_and_off_with_selected_duration`. This unrelated working-tree test was left intact. Python compilation, translation JSON parsing, and whitespace checks passed.
- Remaining limits: local implementation only; no live installation, backup, entity readback, helper migration, or physical-light test was performed. Other uncommitted integration work already exists in this checkout. No publication or deployment was requested.
- Rollback: remove the added button and shared scene-control modules, Scene select class/setup addition, button platform registration, and their translation entries; reload the prior integration version. Original helpers remain available during migration.

## 2026-09-25 — Individual Light Entities controls

- Request: dynamic per-light exclusion toggles and brightness multipliers, omitting WLED master lights.
- Affected objects: switch/number platform setup, new entity_controls module, Chameleon scene/brightness/power handling, WLED segment payloads, README, and focused tests. Existing configuration, light ordering, group IDs, scenes, and consumers are retained. Existing uncommitted work remains in this checkout.
- Actions: expose controls on a linked Light Entities device, since Home Assistant's native device Controls panel cannot define custom subsections. Each configured non-master light defaults to enabled and a 100% multiplier; persistent settings live in config-entry options. Exclusion is serialized with group commands and immediately calls light.turn_off. Subsequent scenes, brightness changes, and group power-off skip excluded lights. Re-enabling reapplies the active scene. WLED native requests target only selected segments and use individual segment brightness with the master at full brightness. Group power-off no longer switches the whole WLED device off, preserving excluded or unconfigured segments. Segment identity uses the existing numbered registry unique-ID convention.
- Validation: full local suite: 80 passed, one pre-existing failure (undefined duration in test_single_led_segment_fades_on_and_off_with_selected_duration). Three new tests cover renamed WLED master filtering, persistent exclusion/immediate turn-off/re-enable, preserved multiplier options, and WLED segment palette/brightness/power isolation. Python compilation and whitespace validation passed.
- Remaining limits: local implementation only; no deployment, publication, Home Assistant backup, live entity readback, or physical-light test. Controls are generated at integration setup and refresh when the configured-light list is reloaded. Defaults do not alter existing configuration. Multipliers range from 0% to 100%; at 0%, an enabled light can still receive palette data. Native WLED requests retain required device-wide transition style and power-on behavior.
- Rollback: revert the individual-control module/platform additions and corresponding light/WLED handling, retaining the existing config entry and IDs; saved per-light options can remain unused by the prior version.

## 2026-09-25 — Publish combined controls and deploy for live validation

- Request: publish all combined local integration changes to the configured GitHub repository and test the live Home Assistant installation.
- Review: combined changes add Scene/Choose Random Scene and individual light controls, replace continuous animation with native WLED transitions, and migrate the config entry to version 5 while preserving the retained controls. Fixed the undefined test duration, preserved per-light options through Configure, and honored the existing Send Palette switch during native updates. Corrected WLED master discovery: masters use the MAC address alone, while suffixes _0, _1, etc. are individual segments. Segment 0 must remain exposed and controllable. A fully managed WLED device powers off with the group; partial management leaves other segments untouched.
- Baseline and recovery: the existing integration loaded successfully. A full Home Assistant backup completed before the migration. Configuration-reference searches found the existing Random colors script and Common Area Accent Scene helper, both referencing retained light/transition controls; no references to retired controls were found. The configured GitHub repository is public despite the earlier project-instruction description; no credentials, endpoints, household configuration, or backup files are included in this publication.
- Local validation: 83 tests passed, Python compilation and whitespace checks passed, translation JSON parsed successfully, and a sensitive-pattern scan of changed files found no credentials or private keys.
- Deployment: pending GitHub push and HACS installation/readback at the time of this entry. Live outcomes will be recorded after testing. Recovery is reinstalling the previous HACS revision; the full backup is available if registry migration needs reversal. No backup restoration is performed as part of this deployment.

### Live validation correction

- The first deployment loaded with all seven segment controls and no setup errors. Scene selection worked, but a manual RGB command revealed that the combined animation-removal change had accidentally removed the _apply_manual_color method declaration while leaving its body under the artwork downloader. Restored that handler, cleared stale success diagnostics, and added tests using the real ChameleonLight command methods for manual colors, multipliers, exclusions, scene routing, and power-off. An additional internal-handler audit found no missing methods.
- Startup initially displayed raw target entity IDs because WLED states were not loaded yet. Control names now fall back to the existing entity/device registries, preserving their unique IDs and already-created entity IDs. A focused test covers startup names for segment 0 and named segments.
- Correction validation: 88 local tests passed, compilation and whitespace checks passed. Live installation and revalidation of this correction follow below.

### Final compatibility correction and live results

- HACS accepted a short commit ID but initially wrote the earlier source files. Readback caught the mismatch; reinstalling the full commit SHA wrote the repaired manual-color handler and registry-backed control names. Future deployment should verify file contents rather than trusting HACS's installed-version label.
- Live tests of the repaired code confirmed 80% group brightness × 50% individual multiplier = 40% (102/255), correct segment-0 control, immediate exclusion, untouched palette slots on the excluded segment, restart persistence, re-enabling, a 0% multiplier keeping its segment dark, and preservation of a directly controlled excluded segment through group power-off/on. Repeated Choose Random Scene presses selected different image scenes and the Scene select mirrored the light. Album Art remained selected with the expected no-current-artwork diagnostic while its source was idle; no active cover-change test was possible without starting playback.
- A final service-compatibility check revealed that the existing Apply Scene service schema rejected its advertised entity_id target. Added validated entity targets to that schema and a test verifying transition-before-scene routing and brightness conversion. Individual-control reapplications now publish the updated group state and diagnostics. Local validation: 90 tests passed, compilation and whitespace checks passed.
- Final correction deployment and restoration readback will be recorded below.

### Completed deployment before the control-layout follow-up

- Installed integration source revision 63f21f2 using its full SHA. Readback of all 11 changed Python source files matched the local committed contents. The retained Chameleon light, transition, and WLED style entity/unique IDs remained unchanged. The main device had the new Scene select and Choose Random Scene button, and the linked Light Entities device exposed seven toggles plus seven multipliers with no WLED masters.
- The corrected targeted Apply Scene service worked live, the group/Scene select read back the original scene at full brightness, and Chameleon system-log checks found no errors after the final restart. Existing script/helper definitions remained unchanged. Returned all tested overrides to Enabled/100% and transition to its prior 0 seconds. Restored each light's previous power, brightness, primary RGB/RGBW color and effect through the native scene API; readback matched the saved baseline for all seven lights and both WLED masters. A fresh playing-media cover change was not tested because the configured player was idle.

## 2026-09-25 — Shorter labels and palette setting in Configure

- Request: place individual light controls with the main Chameleon controls, remove Enabled/Brightness from their visible names, and move Send Palette to WLED into Configure beside the media-player and normalization settings.
- Local actions: shortened control names while retaining their unique/entity IDs; added the palette Boolean setting to Configure with its existing saved default; stopped creating the palette switch and added a version-6 registry migration to retire only that explicitly removed switch. Per-light overrides continue to survive Configure submissions. The panel location is being clarified because a native light popup cannot add a custom section.
- Baseline: configuration-reference search found no consumers of the palette switch. The prior deployment backup exists; a new backup will be taken before this registry migration.
- Validation and deployment: pending final layout selection and live readback. Rollback is reinstalling the prior source while retaining config-entry options, with a full backup available for registry recovery if needed.

- Layout clarification: the user's ESPHome screenshot shows the native device-page Sensors and Configuration panels. Per-light controls will use EntityCategory.CONFIG on the existing Chameleon device, placing them in the separate Configuration panel. Native section headings are fixed, so it cannot be titled Light Entities without custom frontend work. No custom dashboard dependency is added.
- Migration plan: move existing per-light entity registry records from the child device to the retained main device, preserving entity IDs/unique IDs; remove the old child only after it is empty. A fresh full backup completed before this change. No configuration consumers were found for the retired palette switch or the child device. Tests cover retained identities and protecting an unexpectedly nonempty child device.

### Completed layout deployment

- Published and installed source revision 7182c624f285f4f841bb8df0312a4f3ffcb34599 through HACS; Python source readback confirmed the version-6 migration, Configuration category, and removed palette switch before restarting. The integration loaded successfully with one retained Chameleon device, fourteen per-light controls in the Configuration category, shortened display names, and no remaining child device or palette switch. Existing per-light entity IDs and unique IDs were preserved.
- Live tests confirmed immediate segment exclusion and 80% group brightness multiplied by a 50% individual slider giving 102/255 output, while other enabled lights remained at 204/255. Configure successfully saved the palette setting off and back on while preserving the media source, normalization, transition, WLED style, and individual overrides.
- Restored the original scene, all tested controls to enabled/100%, and the previous physical light states. Readback matched the baseline power, brightness, primary RGB/RGBW colors, and effects for all seven individual lights, both WLED masters, and the Chameleon group. Chameleon log search returned no errors. Final local validation: 94 tests passed; compilation and whitespace checks passed. The configured media source remained idle, so an active cover-change test remains unverified.

## 2026-09-25 — Chameleon palette icon

- Request: choose and deploy a representative icon for the primary Chameleon light. Selected mdi:palette-outline to represent extracting artwork colors and applying lighting palettes.
- Affected object: ChameleonLight's default icon only; entity identity, settings, scenes, and consumers remain unchanged. Current integration, registry, HACS source, and configuration references were inspected before deployment. The full backup from the preceding layout migration remains available.
- Validation: Python compilation and whitespace review; installed source revision 09addca through HACS with source readback, restarted Home Assistant, and confirmed the live light exposes mdi:palette-outline. Restored the pre-deployment group scene and physical lights; all seven physical lights matched their baseline power, brightness, primary colors, and effects; the group retained its scene and full brightness. Its reported representative RGB color changed when the scene was reapplied. Rollback: remove the default icon or reinstall source revision 7182c62.

## 2026-09-25 — Consistent WLED shutdown transitions

- Request: use the selected WLED style and transition time consistently for turn-on, turn-off, scene changes, and album-art changes.
- Affected objects: native WLED shutdown payload and ordinary/fallback light command durations. Config entry, entity identities, member order, controls, and consumers remain unchanged.
- Findings: scene, manual-color, turn-on, and album-art routes already share the native transition handler. Shutdown simultaneously set global power and segment power off. WLED processes global power before segment updates; spatial blending can therefore lose the lit source when both are changed. This is a source-supported diagnosis; physical reproduction of the corrected path remains pending.
- Changes: use global power-off alone when all segments are managed; use segment power-off alone for partial management. Both retain selected bs and tt. Preserve segment color, effect, palette, and brightness memory. Ordinary and fallback on/off calls now receive the runtime duration instead of an immediate/default duration.
- Validation: 110 local tests passed; shutdown parameter coverage includes all eight selectable styles and durations 0 and 2.5 seconds; partial-control isolation and single-LED fade fallback tests passed. Whitespace checks passed. Live integration inspection confirmed it is loaded with push_right and 2.5 seconds.
- Limits: local implementation only; no publication or deployment requested. No live writes, backup, restart, physical transition timing test, or active media cover-change test performed. Existing device-wide fade fallback for devices containing a single-LED segment remains in effect for all actions.
- Rollback: revert the shutdown payload and command-duration changes in wled_palette.py and light.py; reinstall the prior source revision if subsequently deployed. No registry or config-entry restoration needed.

## 2026-09-25 — Queue up to ten transition moves

- Request: serialize rapid random-scene button presses, manual scene selections, and album-art changes until each transition finishes; accept ten moves including the active move and discard the eleventh while full.
- Actions: added one bounded FIFO per Chameleon light, shared by turn-on/scene/manual-color, turn-off, sibling reapply, and artwork updates. Random selection occurs when the move executes. The active slot remains reserved through its transition interval; the next move starts after the device requests complete plus the duration captured at execution. Queue admission has no await before the capacity check. Album-art events retain the corresponding Home Assistant media-state snapshot rather than rereading the newest cover. Artwork updates still require Album Art to be active when executed. Exclusion controls retain their existing immediate-off behavior.
- Lifecycle: a Home Assistant background worker drains the queue; failing commands report errors and permit subsequent moves to execute. Unload cancels the worker and pending completions. Queues are transient and are not persisted across reload/restart. Requests beyond capacity return without applying a move.
- Validation: 113 local tests passed. Controlled interval tests verify ten accepted moves, active-slot capacity, discarded overflow, FIFO ordering, exact 2.5-second requested waits, drained-slot reuse, mixed scene/artwork order with distinct cover references, error recovery, and unload cleanup. Existing button/select routing tests also passed. No entity IDs, unique IDs, settings, consumers, or migrations changed.
- Limits: local only; neither this queue nor the preceding shutdown correction has been published or deployed. Physical button-burst/scene/artwork testing is pending. WLED completion is inferred from the configured duration, since no animation-completion event is exposed. Historical media artwork URLs must still be retrievable when their queued move executes; unavailable artwork uses the existing error handling.
- Rollback: revert queue/lifecycle/artwork-snapshot changes in light.py and accompanying tests; no configuration restoration required.

## 2026-09-25 — Latest waiting album art replaces intermediate covers

- Request clarification: finish the active transition, then use the latest album artwork; discard intermediate waiting artwork updates.
- Actions: artwork admission removes any waiting artwork move and resolves its superseded caller without applying colors, then appends the newest cover at its arrival position. The active artwork move remains reserved through its full duration. Explicit button/manual scene requests retain their order and ten-move capacity. Replacement works even when a waiting artwork slot makes the queue full; a queue filled entirely with explicit moves still uses the existing overflow rule. This supersedes the preceding requirement to render each queued artwork reference.
- Validation: 115 local tests passed, including ten successive cover changes during an active transition, only active/latest covers rendered, replacement at full capacity, and preservation of intervening explicit scene requests. Whitespace validation passed.
- Limits: local implementation only; publication, deployment, and physical transition/artwork tests remain pending. Album-art moves still require Album Art to be active at execution and the captured latest cover to be retrievable.
- Rollback: revert latest-artwork queue admission and tests to restore the previous FIFO artwork behavior. Entity/configuration identities remain unchanged.

## 2026-09-25 — Random WLED styles and unused-artwork verification

- Request: ensure unused album covers are not cached; add Random WLED Transition Style, choosing a fresh non-fade style for every palette update while retaining the single-LED fade guard.
- Actions: added persistent random selection with its translated Random label, keeping existing option values and entity identities. Each palette update chooses once from Fairy Dust, Swipe Right/Left, Push Right/Left, Outside In, and Inside Out, sharing that choice across devices. Random sampling may repeat a style. Power-off reuses the last palette's chosen style (default Outside In before any palette update). The existing WLED device-wide single-LED fade guard remains unchanged.
- Artwork evidence: waiting moves contain media-state references, not downloaded image bytes. Superseded artwork is removed before download/extraction. Actual download bytes are held locally only for the applied move; the integration writes no artwork cache or image file. Added a real artwork-handler burst test confirming ten queued covers result in exactly one download and one extraction for the newest cover.
- Validation: 124 local tests passed, including persistence/restoration of Random, fresh selection on each palette update, no fade in the random pool, last-style reuse on shutdown, and single-LED guard coverage for all seven random candidates. JSON labels and whitespace checks passed.
- Limits: local only; publication, deployment, and physical-light/artwork verification remain pending. WLED's style is global, so a device containing a single-LED segment retains the existing device-wide fade fallback.
- Rollback: revert Random option/helper/labels and corresponding tests; saved random options fall back to the earlier default when read by the prior version. No registry migration required.

## 2026-09-25 — Release artwork after extraction

- Request: clear full artwork immediately after extracting the palette while retaining palette information for future animation features.
- Actions: delete the downloaded-image reference in a finally block immediately after palette and optional white-coverage extraction, before processing colors or contacting lights. Explicitly close ColorThief's source image and in-memory stream even when extraction fails. White-coverage extraction now also explicitly closes its stream, source image, and converted RGB image. Retained palette, applied colors, artwork identity, entity settings, and queue behavior remain unchanged.
- Validation: 127 local tests passed; focused checks verify image/stream closure on success and failure, absence of downloaded bytes from the artwork handler before light updates, optional white-coverage extraction, and retained palette after successful application. Whitespace checks passed.
- Limits: local only; no deployment or live memory profiling performed. Reference release allows normal Python memory reclamation, not guaranteed immediate return of process memory to the operating system; active executor work may retain its input until it completes if cancelled.
- Rollback: revert the extraction-resource and artwork-reference cleanup changes. No configuration or registry restoration required.

## 2026-09-25 — Publish and deploy transition improvements

- Request: publish the combined local transition/shutdown, ten-move queue, latest-artwork, Random style, and artwork-resource cleanup changes to GitHub and the local Home Assistant server, including live testing.
- Preflight: GitHub fast-forward pull found no upstream changes. Live integration loaded; controls, seven configured lights, options, HACS source, and current automation/helper consumers inspected. Full backup completed before deployment. Configuration check passed on Home Assistant Core 2026.8.3. Physical light/setting baselines captured privately in the session; no credentials, cover URLs, or backup contents included in Git.
- Validation: 127 local tests passed; staged source and filenames reviewed for secrets. Publication/deployment/readback/live results follow below. Recovery is reinstalling prior full source SHA 09addca94711ab4633ff452d0b6b321f103cb8e8 and restarting; retained entry/entity IDs need no migration. Full backup is supplementary recovery, not an automatic restore.

### Deployment and live results

- Published runtime source 29d26258f5c1b5b1ce7b957c1d056e8531d6d54c and installed its full SHA through HACS. All eight changed Python source files read back exactly matching local contents before restart. Configuration check passed, and the existing integration loaded after restart with retained entity IDs/settings and Random in the WLED style select.
- A native Home Assistant temporary script issued eleven parallel random-button presses. Recorded exactly ten resulting palette updates; successive updates were 2.755–4.514 seconds apart with the configured duration at 2.5 seconds. The script completed and was removed. An earlier connector-driven burst staggered requests, so it was not used as evidence of overflow rejection. Manual Aquatic then Beach Sunset selections succeeded and the Scene select mirrored the group.
- Native WLED state showed Push Right (16) on multi-LED controllers and Fade (0) on single-LED devices during a Random palette application. Shutdown preserved its last chosen style; controller-estimated output fell gradually from 2500 through 2311 and 721 to idle output after shutdown, supporting a rendered transition rather than immediate cutoff. Global master shutdown intentionally retains segment on flags; HA may show segment entities logically on while their WLED master is off.
- User advanced tracks. Recorded automatic artwork updates while Album Art was active and applied colors on all seven lights. One cover was rejected by the vivid-color filter; temporarily disabling it allowed extraction/application, and the final cover succeeded after the original filter was restored. Exact intermediate-cover download suppression and byte release remain verified by tests/source, not live memory instrumentation.
- Restored Inside Out, 2.5 seconds, vivid-color filtering on, full group brightness, and Album Art on with the latest user-selected track. All seven lights read back on with applied colors and no Chameleon error. The original physical RGB colors were superseded by the user's intentional track changes. Existing quad-button automation and Store Restore Lights helper remain unchanged. Chameleon system-log search returned no entries.
- GitHub hassfest passed. CI uncovered fixture cleanup and real-package/mock compatibility issues; corrected tests to unload queue workers and consistently stub the button/use the actual exception class. Local lint and all 127 tests pass after those corrections. HACS's general-store validation reports pre-existing missing license and repository topics; custom repository installation worked. No license was invented because the local upstream checkout provides none. These metadata checks do not establish a runtime fault.

### HACS update-notice correction

- After the follow-up documentation/test commit, HACS initially reported a pending update. Installing the full SHA still left the notice on because installed_version held the 40-character SHA while latest_version held its seven-character abbreviation. Reinstalling the matching abbreviated revision cleared update.chameleon_update_2 to off, with installed/latest both 377b725. All eight changed runtime source files remained identical to the already loaded deployment, so another restart was unnecessary.
- For this repository's commit-based updates: publish final notes/tests first, install using the HACS available-version format, and verify file contents plus the update entity. Full-SHA readback remains useful when diagnosing stale downloads, but its metadata format can cause a false pending update.
- This final documentation-only publication will be installed using its matching HACS revision after pushing, without changing the validated runtime code or restarting Home Assistant.

## 2026-09-26 — Preserve the scene dropdown while the group is off

- Request: keep `select.chameleon_common_area_scene` showing the previously selected scene when the Chameleon light is off, and restore that scene on turn-on.
- Affected objects/actions: added a public `selected_scene` property to the Chameleon light that returns its active effect or remembered `_last_effect`. The Scene select reads this property. The existing turn-off handling still clears the active effect while keeping the remembered scene; bare turn-on already restores the remembered scene. Manual colors retain the remembered scene in the dropdown as well. Entry/entity IDs and consumers are unchanged.
- Validation: 131 local tests passed. New regression cases exercise actual turn-off and bare turn-on for named scenes and Album Art, checking the dropdown throughout; an untouched light still has no selected scene. Python compilation and whitespace checks passed.
- Publication: user authorized committing and pushing this fix to the configured GitHub main branch, and will install the update through Home Assistant Store themselves. Remote preflight confirmed local HEAD matches origin/main; the six-file diff was reviewed for secrets and the full suite passed again (131 tests).
- Remaining limits: no live deployment or restart was performed for this fix; live entity readback is pending the user's installation. Remembered scenes survive power toggles within the running integration; persistence across integration reloads or Home Assistant restarts is outside this change.
- Rollback: revert the new light property and select property change, along with their tests and README update, then install the previous integration code.

## 2026-09-26 — Accept dark artwork; reject only pure black

- Request: allow the current dark green cover through Only Interesting Colors; reject only pure black.
- Evidence: live Common Area Accent Lights is on Album Art with normalization enabled and reports No vivid colors found. Privately inspected current Tigallerro artwork: dominant (13, 27, 20), hue 150 degrees, rejected by the old value threshold. The updated filter accepts it; normalization yields (89, 255, 172) with the same hue.
- Actions: retain every non-black swatch, including gray, muted, skin tones and repeated hues. Remove brightness normalization's darkness cutoff. Check original pixels for entirely black images before ColorThief rounds black to nonzero gray. Preserve mostly-white balancing; update no-color errors and README. No IDs, consumers or saved settings changed.
- Validation: 139 local tests passed; regressions cover true-black image quantization, near-black colors, dark green hue preservation and the observed cover swatch. Source lint and whitespace checks passed. Repository fast-forward pull found it up to date.
- Limits: local implementation only; not published or installed. Live light application remains unverified. Normalization brightens the green rather than reproducing the background's darkness. No live device changes or restart performed.
- Rollback: restore prior color_extractor.py/light.py files and reinstall/restart if deployed; no registry migration.

## 2026-09-26 — Avoid black flash before animated shutdown

- Request: turning off the main Chameleon light briefly flashes black before the off animation.
- Evidence: current shutdown uses global WLED power-off. WLED's renderer documents a race where global brightness changes before the transition snapshot exists, blanking a frame: https://github.com/wled/WLED/blob/main/wled00/FX_fcn.cpp (global On/Off transition workaround). This matches the reported symptom; installed firmware and physical output have not been inspected for this change.
- Actions: send segment off flags with the chosen style and duration while leaving master brightness untouched; wait for the native animation, then send master off with zero transition only when all device segments are controlled. Partial control leaves master power untouched. Preserve colors, effects, brightness, single-LED fade guard, disabled-member exclusions and remembered scene. Ordinary lights begin shutdown concurrently with WLED. Queue timing credits time already spent animating, avoiding a second full cooldown, including brightness-zero and Off-effect requests. Failed native requests retain service fallback.
- Validation: 146 local tests passed; regression cases cover ordering across the animation wait, whole/partial device control, zero duration, all supported styles, single-LED fade, master-finalization failure, concurrent ordinary-light shutdown and no double queue delay. Source/test lint and Git whitespace checks passed.
- Limits: local code only; not committed, published or installed. Live Home Assistant readback and physical verification remain pending. Main Chameleon off now completes after the animation, and fully controlled WLED segment flags remain off afterward until turn-on restores them. Existing uncommitted dark-artwork changes were preserved.
- Rollback: revert this entry's staged shutdown and queue-duration changes in light.py/wled_palette.py and corresponding tests; retain unrelated artwork changes. No entry/entity ID, saved setting or automation migration is required.

## 2026-09-26 — Install and test combined artwork, selector and shutdown fixes

- Request: install the dark-artwork change alongside the scene-selector and power-off flash fixes, and test all three on the server while the user keeps the album playing.
- Preflight: 146 tests and source/test lint passed. Remote main matches local HEAD 92ea450. Inspected loaded entry, controls, member lights, HACS source and consumers. Full Home Assistant backup completed before installation; Core 2026.8.3 configuration check passed. Retain existing entry/IDs/options, transition 2.5 seconds, Random WLED style and enabled light controls.
- Installation path: direct server authentication unavailable; publish reviewed combined source to the configured private repository and install its exact revision through HACS. No registry or consumer migration required. Scene-selector fix already present in committed HEAD. Runtime deployment and live results follow after verification.
- Recovery: reinstall previous HACS revision e045550 (or selector-only 92ea450) and restart; full backup is supplementary recovery requiring separate restore authorization. Live test will finish on Album Art using the user's playing cover.
